import React from "react";
import { render } from "@testing-library/react";

import AccountVerify from "../Pages/User/Account Verify";

describe("AccountVerify", () => {
  test("renders App component", () => {
    render(<AccountVerify />);
  });
});
